
interface ModalProps {
    id: number | undefined
    isModalOpen: boolean
    type: string
    closeModal: () => void
}

const Modal = ({ isModalOpen, type, closeModal, id }: ModalProps) => {
    return (
        <></>
    )
}